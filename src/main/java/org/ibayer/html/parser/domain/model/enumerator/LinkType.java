package org.ibayer.html.parser.domain.model.enumerator;

/**
 * Functional selection of link.<br/>
 * Internal links are referenced as same domain <br/>
 * External links are referenced as different domain(s).
 * @author ibrahim.bayer
 *
 */
public enum LinkType {
	INTERNAL,EXTERNAL;
}
